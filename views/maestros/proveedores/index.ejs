<!DOCTYPE html>
<html lang="en">
  <%- include ('../../partials/head') %>
  <body class="flex">
    <%- include ('../../partials/sidebar') %>
    <main
      class="flex flex-grow flex-col gap-4 bg-gray-100 p-8 text-xl font-semibold"
    >
      <h1 class="text-2xl">Proveedores</h1>
      <div class="bg-white rounded-md shadow-sm p-4">
        <table
          class="mt-3 min-h-[150px] w-full text-nowrap text-left text-sm text-gray-500"
        >
          <thead class="bg-gray-100 text-sm uppercase text-gray-600">
            <tr>
              <th class="px-4 py-3">Código</th>
              <th class="px-4 py-3">Descripción</th>
              <th class="px-4 py-3">RUC</th>
            </tr>
          </thead>
          <tbody>
            <% for(let proveedor of proveedores) {%>
            <tr class="border-b bg-white hover:bg-gray-100">
              <% for(let cell of proveedor) { %>

              <td class="px-4 py-3"><%= cell %></td>
              <% } %>
              <!-- <td>
              <a
                href="/maestros/proveedores/editar/<%= proveedor[0] %>"
                class="text-blue-500"
                >Editar</a
              >
              <a
                href="/maestros/proveedors/eliminar/<%= proveedor[0] %>"
                class="text-red-500"
                >Eliminar</a
              >
            </td> -->
              <% } %>
            </tr>
          </tbody>
        </table>
        <div class="overflow-x-auto"></div>
      </div>
    </main>
  </body>
</html>
